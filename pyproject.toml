[project]
name = "chessbook"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["poethepoet>=0.27"]

[tool.poe.tasks]
up    = "docker compose up --build"
down  = "docker compose down"
logs  = "docker compose logs -f"
clean = "docker compose down -v"
